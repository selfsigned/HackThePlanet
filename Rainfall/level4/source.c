#include <stdio.h>
#include <stdlib.h>

unsigned int m;

void sym.n(void)
{
	uint s;

	sym.imp.fgets(&s, 0x200, stdin);
	sym.p(&s);
	if (m == 0x1025544) {
		system("/bin/cat /home/user/level5/.pass");
	}
	return;
}

void main(void)
{
	n();
	return;
}
